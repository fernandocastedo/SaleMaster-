spring.application.name=SalesMaster

# Server Configuration
# Puerto dinámico para Render.com (usa PORT de variables de entorno, o 8080 por defecto)
server.port=${PORT:8080}

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/salesmasterdb
spring.datasource.username=postgres
spring.datasource.password=catetopositive

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
